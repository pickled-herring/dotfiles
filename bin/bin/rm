#!/bin/sh
# the system's rm can still be accessed via /usr/bin/rm
# Use for not deleting files unless I actually need it gone
if [ "$1" = "" ]; then
		echo "usage: rm <file>";
		exit 1;
fi
if [ ! -d $HOME/.trash ]; then
		mkdir $HOME/.trash;
fi
mv $1 $HOME/.trash
